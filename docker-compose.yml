# ScyllaDB with Alternator (DynamoDB-compatible API) for Coolify + Traefik.
# Set SCYLLA_HOST in .env to your public hostname before deploying.

services:
  scylla:
    image: scylladb/scylla:latest
    container_name: scylla-alternator
    restart: unless-stopped
    command:
      - --smp
      - "1"
      - --memory
      - "2G"
      - --overprovisioned
      - "1"
      - --alternator-port
      - "8000"
      - --alternator-address
      - "0.0.0.0"
      - --alternator-write-isolation
      - always
      - --alternator-enforce-authorization
      - ${ALTERNATOR_AUTH_ENABLED:-false}
      - --alternator-warn-authorization
      - ${ALTERNATOR_AUTH_WARN:-false}
      # CQL auth — required to create Alternator users (roles stored in scylla-data volume)
      - --authenticator
      - PasswordAuthenticator
      - --authorizer
      - CassandraAuthorizer
    volumes:
      - scylla-data:/var/lib/scylla
    expose:
      - "8000" # Alternator (DynamoDB API) — routed via Traefik
      - "9042" # CQL — internal only, not exposed via Traefik
    healthcheck:
      # GET / returns "healthy: <host>:8000" (no auth required)
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:8000/ | grep -q '^healthy:'"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    labels:
      - traefik.enable=true
      - traefik.http.routers.scylla-alternator.rule=Host(`${SCYLLA_HOST}`)
      - traefik.http.routers.scylla-alternator.entrypoints=https
      - traefik.http.routers.scylla-alternator.tls=true
      - traefik.http.routers.scylla-alternator.tls.certresolver=letsencrypt
      - traefik.http.services.scylla-alternator.loadbalancer.server.port=8000
      - traefik.http.services.scylla-alternator.loadbalancer.healthcheck.path=/
      - traefik.http.services.scylla-alternator.loadbalancer.healthcheck.port=8000
      - traefik.http.services.scylla-alternator.loadbalancer.healthcheck.scheme=http
      - traefik.http.services.scylla-alternator.loadbalancer.healthcheck.interval=30s

  scylla-bootstrap:
    build: ./bootstrap
    user: "0:0"
    depends_on:
      scylla:
        condition: service_healthy
    volumes:
      - scylla-data:/var/lib/scylla
      - admin-secrets:/secrets
    environment:
      - SCYLLA_CQL_PASSWORD=${SCYLLA_CQL_PASSWORD:-}
    entrypoint: ["/bin/bash", "-c"]
    command:
      - "set -euo pipefail; source /bootstrap/cql-password.sh"
    restart: "no"

  scylla-admin:
    build: ./admin-ui
    restart: unless-stopped
    depends_on:
      scylla:
        condition: service_healthy
      scylla-bootstrap:
        condition: service_completed_successfully
    volumes:
      - admin-secrets:/secrets:ro
    environment:
      - SCYLLA_CQL_HOST=scylla
      - SCYLLA_CQL_PORT=9042
      - SCYLLA_CQL_USER=cassandra
      - SCYLLA_CQL_PASSWORD_FILE=/secrets/cql-password
      - SCYLLA_CQL_PASSWORD=${SCYLLA_CQL_PASSWORD:-}
      - ALTERNATOR_ENDPOINT=http://scylla:8000
      - ADMIN_USERNAME=${ADMIN_USERNAME}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - ADMIN_PASSWORD_HASH=${ADMIN_PASSWORD_HASH:-}
      - SESSION_SECRET=${SESSION_SECRET:-}
    expose:
      - "3000"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:3000/healthz >/dev/null"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    labels:
      - traefik.enable=true
      - traefik.http.routers.scylla-admin.rule=Host(`${ADMIN_UI_HOST}`)
      - traefik.http.routers.scylla-admin.entrypoints=https
      - traefik.http.routers.scylla-admin.tls=true
      - traefik.http.routers.scylla-admin.tls.certresolver=letsencrypt
      - traefik.http.services.scylla-admin.loadbalancer.server.port=3000

volumes:
  scylla-data:
  admin-secrets:
